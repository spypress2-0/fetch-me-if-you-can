import React from 'react';

const Default =  (props) => {
    return (
        <div>
            <p>
            Currently connected to server (insert your server number here),
            awaiting server activity
            </p>
        </div>
    )
}
export default Default;